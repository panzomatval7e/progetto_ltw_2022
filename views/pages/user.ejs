<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="/css/photo.css"/>
    <link rel="stylesheet" type="text/css" href="/css/navbar.css"/>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Photo Community</title>
</head>
<body>

    <!-- NAVBAR -->
    <%- include('../partials/navbar_image', {sessione: sessione}); %>

    <!-- IMMAGINE PROFILO E DATI -->
    <div class="container pt-3">
        <div class="row">
            <div class="col-md">
                <img src="../images/<%= risultato_user[0].immagine %>">
            </div>
            <div class="col-md">
                <h3>Username: <%= user %></h3>
                <h3>Nome: <%= risultato_user[0].nome %></h3>
                <h3>Cognome: <%= risultato_user[0].cognome %></h3>
                <h3>Mail: <%= risultato_user[0].mail %></h3>
            </div>
        </div>
    </div>

    <!-- IMMAGINI CARICATE -->
    <div class="container pt-3">
        <ul class="photo_ul">
            <% for (let i = risultato_img.length-1; i >=0; i--) { %>
            <li class="photo_li">
                <a href="/image/<%=  risultato_img[i].immagine %>"><img class="photo_img" src="../images/<%= risultato_img[i].immagine %>" loading="lazy"></a>
            </li>
            <% } %>
            <li class="photo_li"></li>
        </ul>
    </div>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="/css/photo.css"/>
    <link rel="stylesheet" type="text/css" href="/css/navbar.css"/>
    <link rel="stylesheet" type="text/css" href="/css/image.css"/>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <%- include('../partials/back-to-top-script') %>
    <title>Photo Community</title>
</head>
<body>

    <!-- NAVBAR + BACK TO TOP-->
    <%- include('../partials/navbar_image', {sessione: sessione}); %>

    <!-- IMMAGINE E INFO-->
    <div class="container pt-3">
        <div class="row">
            <div class="col-lg-9">
                <img src="../images/<%= tagID %>" class="img-fluid" loading="lazy">
            </div>
            <div class="col-lg">
                <h3>Photographer: <a class="link_user" href="../user/<%= risultato[0].username %>"><%= risultato[0].username %></a></h3>
                <h3>Camera: <%= risultato[0].camera %></h3>
                <h3>Lens: <%= risultato[0].lente %></h3>
                <h3>Focal length: <%= risultato[0].distanza %></h3>
                <h3>ISO: <%= risultato[0].iso %></h3>             
                <h3>F: <%= risultato[0].f %></h3>
                <h3>Shutter speed: <%= risultato[0].shutter_speed %></h3>
            </div>
        </div>
    </div>
    
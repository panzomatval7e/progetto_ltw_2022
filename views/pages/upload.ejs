<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/css/profile.css"/>
    <link rel="stylesheet" type="text/css" href="/css/navbar.css"/>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Upload photo</title>
    <script>
        function preview() {
            frame.src = URL.createObjectURL(event.target.files[0]);
        }
    </script>
    <%- include('../partials/back-to-top-script') %>
</head>
<body>

    <!-- NAVBAR + BACK TO TOP-->
    <%- include('../partials/navbar', {sessione: sessione}); %>

    <div class="container pt-5">
        <div class="row">
            <div class="col-md">
                <form action="/uploadImage" method="POST" enctype="multipart/form-data">
                    <% if (message) { %>
                        <div style="text-align: center" class="alert alert-<%=message.type%>">
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                            <strong><%= message.intro %></strong> <%= message.message %>
                        </div>
                    <% } %>
                    <input class="form-control" type="file" name="image" id="formFile" onchange="preview()">
                    <input type="text" class="form-control mt-3" name="camera" placeholder="Camera">
                    <input type="text" class="form-control mt-3" name="lens" placeholder="Lens">
                    <input type="text" class="form-control mt-3" name="focal_length" placeholder="Focal length">
                    <input type="text" class="form-control mt-3" name="ISO" placeholder="ISO">
                    <input type="text" class="form-control mt-3" name="F" placeholder="F">
                    <input type="text" class="form-control mt-3" name="shutter_speed" placeholder="Shutter speed">
                    <select class="form-select mt-3" name="categoria">
                        <option selected>Categories</option>
                        <option value="portrait">Portrait</option>
                        <option value="landscape">Landscape</option>
                        <option value="sport">Sport</option>
                        <option value="macro">Macro</option>
                        <option value="food">Food</option>
                        <option value="animal">Animal</option>
                      </select>
                    <button type="submit" class="btn btn-black mt-3 mb-3">Click to upload</button>
                </form>
            </div>
            <div class="col-md">
                <img id="frame" src="" class="img-fluid">
            </div>
        </div>
    </div>

    
</body>
</html>